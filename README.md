# wordle_solver
Wordle Game and Algorithmic solver

exec1.out is a runnable wordle game
exec2.out runs the algorithm
exec3.out finds the number of words solved as well as the average move count

to make them use make <target>
to clean the executables use make clean
